%h1 Armour
%table#characters
	%thead
		%tr
			%th Armour
			%th Cost
			%th Armour Class (AC)
			%th Strength
			%th Stealth
			%th Weight
			%th ID
	%tbody
		- sort = nil
		- armour_categories(@armour).each do |item|
			- if sort != item.category
				%tr
					%td
						%strong= item.category.scan(/armour,\s(.*)_armour/)[0][0].capitalize	
			- sort = item.category
			%tr
				%td= item_name_to_link(item, nil)
				%td= cost_converter(item.cost)
				%td= display_armour_class(item)
				%td= if !item.strength then "---" else item.strength end
				%td= "Disadvantage" if item.stealth
				%td= weight(item.weight)
				%td= item.id