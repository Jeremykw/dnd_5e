.show
  = render partial: "characters/show" # show saved character states

.new
  -# Show Edit Character abilities form
  %h1 Abillities
  = new_ability_suggestions(@character.character_class)
  = form_tag character_ability_path(@character, @character.ability), :method => :put do
    = render partial: "new_or_edit"  # Show edit character form
